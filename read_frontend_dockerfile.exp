#!/usr/bin/expect -f

set timeout 30
set password "k6kdDfT8"
set host "217.154.191.3"
set user "root"

spawn ssh -o StrictHostKeyChecking=no $user@$host "cat /opt/bestkids-game/frontend/Dockerfile"
expect "password:"
send "$password\r"
expect eof
